# README Onboarding Visual Design

## Evidence

A current local browser run captured the deliberately constructed demo from the first-use screen
through confirmed criteria and deterministic analysis. The evidence matrix is the strongest single
frame because it shows the product's core distinction between requirement, provisional status,
evidence level, and unresolved human resolution.

The screenshot is controlled product-flow evidence only. It is not a customer case, a real public-PR
review, runtime verification, or proof of correctness.

## Decision

Add the accepted 1280 by 720 evidence-matrix screenshot near the top of README. Place an explicit
constructed-demo disclosure immediately below it and link to the documented demo section. Use
plain Markdown so the asset renders on GitHub without a service, tracker, or external host.

Do not alter the workbench, create a synthetic testimonial, or add decorative generated imagery.

## Verification

Repository contract tests require the tracked JPEG, README link, nearby disclosure, and meaningful
alternative text. The focused contracts, full suite, Ruff, deterministic benchmark, and diff checks
remain the acceptance gates.
