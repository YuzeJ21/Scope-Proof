# ScopeProof v0.2.3 platform and package matrix

Date: 2026-07-26  
Verification base: `482d1a78f5d3345029d35d33a518ca36bc2f8e29`  
Classification: internal engineering evidence only

## Runtime matrix

| Environment | Result | Evidence and limit |
| --- | --- | --- |
| macOS with Python 3.12.0 | Pass | Full checkout suite, coverage gate, Ruff, both constructed benchmarks, R-002 offline run, build, isolated installation, representative review, exports, and installed workbench health completed. |
| Python 3.11 | Not executed in this environment | No Python 3.11 interpreter was installed locally. CI contracts continue to define 3.11 as the compatibility floor. |
| Python 3.13 | Not executed in this environment | No Python 3.13 interpreter was installed locally. |
| Linux desktop | Not executed in this environment | No Linux environment was available. |
| Windows desktop | Not executed in this environment | No Windows environment was available. |

This matrix does not infer platform support from pure-Python packaging metadata
or from CI configuration.

## Package build

Command:

```bash
uv build --out-dir /tmp/scopeproof-v023-goal-tMwRby
```

Results:

| Artifact | Entries | SHA-256 |
| --- | ---: | --- |
| `scopeproof-0.2.3-py3-none-any.whl` | 98 | `d2b49d7b42a64a90d15984ed5cda4ba73ce34e3837a701db17b801c51d3a0c42` |
| `scopeproof-0.2.3.tar.gz` | 517 | `8e54cc10198a6e5c5d20f30c989d8382a3cfc83c35f7b8194e3dde0fc78d51b7` |

Inventory inspection found no `.scopeproof` review storage, coverage data,
virtual environments, Git metadata directory, Python cache directory, or
bytecode in either distribution. The source distribution intentionally includes
repository documentation, tests, and GitHub configuration; those are source
files, not a packaged `.git` directory.

## Isolated wheel installation

The wheel installed into a fresh Python 3.12 virtual environment outside the
checkout with 47 packages. The isolated CLI reported `scopeproof 0.2.3`.

The installed package then:

- completed the 12-case deterministic benchmark without mismatches or
  must-have False Ready outcomes;
- completed the two-case comparison benchmark with exact Added, Modified,
  Relocated, Removed, and Unchanged counts;
- created and persisted a four-criterion review at `head-demo-002`;
- returned the conservative `blocked` verdict with AC-02, AC-03, and AC-04
  blocking;
- reopened the record through the installed storage/schema path for export;
- produced non-empty Markdown, CSV, and HTML output;
- produced JSON that validated as `ReviewState`, preserving four criteria,
  `head-demo-002`, and the `blocked` verdict; and
- launched the installed workbench on loopback, where
  `/_stcore/health` returned `ok`.

The packaged page contained the candidate's explicit `:focus-visible` rule.
This confirms package inclusion, not successful keyboard-only operation.

## Accessibility matrix

| Check | Result |
| --- | --- |
| Semantic labels and reading order | Partial browser-snapshot evidence; no screen reader |
| Sampled body and link contrast | Pass at 18.11:1 and 6.58:1 |
| Visible focus rule in installed package | Present; complete keyboard flow not executed |
| Narrow viewport at 390 x 844 | Pass for the sampled evidence-matrix route |
| Keyboard-only completed review | Not executed in this environment |
| VoiceOver, NVDA, or JAWS | Not executed in this environment |
| 200% browser zoom | Not executed in this environment |
| WCAG conformance | Not claimed |

The detailed evidence and screenshots are in the
[first-use accessibility audit](../audits/v0.2.3-workbench/accessibility-and-first-use-audit.md).

## Checkout gates

- Complete offline suite: 1,589 passed and one intentional skip.
- Combined `scopeproof_core` and `apps` coverage: 94.78%, displayed as 95% by
  the configured zero-decimal coverage gate; `--fail-under=95` passed.
- Ruff: passed.
- Repository contracts: 63 passed before the final status alignment and 64
  version/status-focused checks passed afterward.
- Deterministic benchmark: 12/12 cases, zero mismatches, zero must-have False
  Ready.
- Comparison benchmark: 2/2 cases, zero mismatches.
- R-002: 20/20 cases, zero failures, zero skips, zero normalized rerun
  mismatches, zero unexpected Ready outcomes, zero immutable-reference errors,
  and zero implementation/test separation errors.

Frozen tracked R-002 hashes remained:

- source manifest:
  `b2acd73b4695ff20ae31723199691374621b0258cccc76f4e807aee66044e4bb`;
- criteria:
  `bb62a7f5bd891322cfcf7ef6681d61dec63b8d2c28078e2aef35a756e1485ece`;
- candidate labels:
  `e1ba509a9569195d68e718708f2585941052a7eeb2099d46a3697963f8a2abe8`;
- recorded result:
  `2fc7115a6ffee3143d0f1d93dca1d51359f40857ec89f1c180d04d7f1e2d359f`;
- summary:
  `e64ce423c0ede72bc3e29f0c23ffc2ba40928b8860191b0553e5cc88a7b75a03`.

The fresh R-002 run records the current ScopeProof commit as provenance. That
expected provenance field means its complete emitted JSON is not claimed to be
byte-identical to the historical recorded result. The tracked frozen inputs and
record remain byte-identical, and the benchmark's normalized rerun mismatch
count is zero.

## Evidence boundary

These results establish current-environment engineering reproducibility only.
They do not establish correctness of public PRs, participant usability,
customer value, market demand, commercial readiness, external platform
support, or Stage 1–4 progress.
