# ScopeProof v0.2.3 platform and package matrix

Date: 2026-08-02
Merged product commit: `2a320df966eff30c05a2b1dce607a247201fa165` (PR #180)
Merged product tree: `add81a2d0ba7e64f8e4318a1959bbe7e6e4acfc8`
Independently verified PR head: `ed9f9c0cf6b7cf7cc25403d6138e7a8391f55e0f`
Verified PR-head tree: `add81a2d0ba7e64f8e4318a1959bbe7e6e4acfc8`
Public install and latest release: v0.2.1
Historical local checkout verification head: `87e95a76ca4100458465da520049992e8b39af5c`

Historical package build base: `482d1a78f5d3345029d35d33a518ca36bc2f8e29`
Classification: internal engineering evidence only

## 2026-08-02 merged product-tree package verification

PR #180 merged the independently verified head's exact product tree as
`2a320df966eff30c05a2b1dce607a247201fa165`. The following fresh results belong
only to clean implementation head
`ed9f9c0cf6b7cf7cc25403d6138e7a8391f55e0f` and shared tree
`add81a2d0ba7e64f8e4318a1959bbe7e6e4acfc8`. This later documentation alignment
is not part of either artifact and changes the repository tree and source
distribution.

| Candidate artifact | Size | Inventory | SHA-256 |
| --- | ---: | ---: | --- |
| `scopeproof-0.2.3-py3-none-any.whl` | 248,171 bytes | 99 entries | `70bdca1a0d609c81ac8cd2274dc4915612067cfdb1c2205276faafd7c6358ac8` |
| `scopeproof-0.2.3.tar.gz` | 5,818,424 bytes | 534 entries | `7fff8ba0b6b6c85ae0f22fe487762de8a525d04145c50eab46792233b798573e` |

Both inventories had zero forbidden-path matches. The wheel installed into a
new Python 3.12 virtual environment outside the checkout with 48 installed
packages; `pip check` reported no broken requirements. `scopeproof --version`
and `scopeproof-web --version` both reported 0.2.3. Both installed benchmarks
completed with zero mismatches, and the installed workbench health endpoint
returned exact body `ok`. The server, listener, build directory, virtual
environment, isolated home, coverage file, test caches, Ruff cache, and Python
bytecode were removed after evidence capture.

No browser walkthrough, keyboard-only completion, real screen reader, actual
200% zoom, Python 3.13, Windows desktop, or Linux desktop run was performed on
the verified product tree. The earlier PR #179 pointer/browser evidence remains
historical and must not be attributed to PR #180 or its product tree. Exact
commands and boundaries are in the
[exact-head verification audit](../audits/exact-head-runtime-evidence/verification.md).

## Historical 2026-07-31 public-main continuation

Stage 0 integrity repairs merged via PR #177 at
`3d88808f12f46b68059b9e89c40c7ccd595d9032`. Exact-main CI, CodeQL, and
Pages passed. The release-alignment branch passed 1,633 tests with one
intentional skip, the 95.10% coverage gate, Ruff, repository contracts, the
12-case benchmark, and the two-case comparison benchmark.

Two release-alignment builds were byte-identical.
Artifact inventory: 98 wheel entries and 520 source-distribution entries.
The pre-record verification hashes were:

- wheel:
  `b9be88867c4880058d822fd7af17c736077210151fecd8b4e191d1af1bdd12c4`;
- source distribution:
  `815ca6567fa605688abf2faea40d5169a21a54648a14a4a10d93e2d8c9ffb838`.

Recording this evidence changes the source distribution because this document
is included in it. A publication workflow must therefore rebuild and checksum
the final merged `main`; these hashes are verification evidence, not release
assets.

The wheel installed outside the checkout with 47 compatible packages.
ScopeProof and both console commands reported 0.2.3. Both installed benchmarks,
R-002 help, a conservative four-criterion review, four exports, list, delete,
and loopback health passed.

The pointer-operated installed-workbench flow passed demo loading, criteria
confirmation, deterministic analysis, evidence-matrix and criterion-detail
inspection, save, reopen, and Markdown download. The browser found accessible
names for all 119 interactive controls, zero duplicate IDs, and no console
warning or error; keyboard-only completion remains unverified, actual 200%
browser zoom remains unverified, and the requested 390-by-844 viewport
override did not take effect; the captured browser remained 1280 by 720.
Zoom status: actual 200% browser zoom remains unverified.

The installed wheel also completed 20/20 R-002 cases with zero failures,
skips, normalized rerun mismatches, unexpected Ready outcomes, immutable
reference errors, or implementation/test separation errors. No target
repository code was executed. This remains engineering evidence and
contributes zero Stage 1 credit.

Historical hashes below remain evidence for their named source trees and are
not presented as hashes of current `main`.

## 2026-07-30 Stage 0 repair verification

The `codex/v023-readiness-sync` repair tree completed two independent
deterministic builds with byte-identical wheel and source-distribution output.
The wheel contained 98 entries and the source distribution contained 520;
inventory inspection found no review storage, coverage database, Git metadata,
virtual environment, cache directory, or bytecode.

The wheel was installed outside the checkout into a fresh Python 3.12
environment. Dependency compatibility passed, both installed benchmark
commands completed without mismatches, and the installed package created,
saved, reopened, schema-validated, and exported a representative conservative
`blocked` review in JSON, Markdown, CSV, and HTML. An in-process installed
workbench first-use flow loaded the constructed demo, confirmed criteria, ran
analysis, and produced four findings. This is installed-package engineering
evidence, not real-browser, keyboard, assistive-technology, customer, or
runtime-correctness evidence.

The earlier pre-repair continuation at
`f836d64a9b9b6bf63b7dddddabb49ca892f4f91d` is recorded in the
[post-merge release-readiness audit](v0.2.3-post-merge-release-readiness.md).
The 2026-07-31 public-main section of that audit supersedes the historical status
while preserving the earlier evidence below.

On 2026-07-28, the exact public draft head
`2d3bd6afba8435b0bdfce2eb8122ed7bee87eb91` passed protected Python 3.11
compatibility, locked-environment, `verify`, and CodeQL checks. A fresh local
Python 3.12 baseline at the same head passed 1,592 tests with one intentional
skip. This supplements, rather than rewrites, the historical package evidence
below.

A later local continuation at source and dependency head
`5ba6cc6968666c6098b930f5cdffc2503d3d2033` rebuilt both distributions,
installed the wheel into another clean Python 3.12 environment, and repeated
the installed CLI, benchmark, comparison, export, and loopback workbench
checks. That continuation is summarized separately below because its final
documentation-only alignment commit had not yet been created.

## Historical runtime matrix

| Environment | Result | Evidence and limit |
| --- | --- | --- |
| macOS with Python 3.12.0 | Pass | The 2026-07-31 release-alignment checkout suite, coverage gate, Ruff, both constructed benchmarks, R-002 rerun, build, isolated installation, representative review, exports, installed workbench health, and pointer-operated browser flow completed. |
| Python 3.11 | Pass in protected PR CI; not executed locally | The exact public draft head passed the Python 3.11 compatibility job; no local Python 3.11 desktop flow was executed. |
| Python 3.13 | Not executed in this environment | No Python 3.13 interpreter was installed locally. |
| Linux desktop | Not executed in this environment | No Linux environment was available. |
| Windows desktop | Not executed in this environment | No Windows environment was available. |

This matrix does not infer platform support from pure-Python packaging metadata
or from CI configuration.

## Package build

Command:

```bash
uv build --out-dir /tmp/scopeproof-v023-goal-tMwRby
```

Results:

| Artifact | Entries | SHA-256 |
| --- | ---: | --- |
| `scopeproof-0.2.3-py3-none-any.whl` | 98 | `d2b49d7b42a64a90d15984ed5cda4ba73ce34e3837a701db17b801c51d3a0c42` |
| `scopeproof-0.2.3.tar.gz` | 517 | `8e54cc10198a6e5c5d20f30c989d8382a3cfc83c35f7b8194e3dde0fc78d51b7` |

Inventory inspection found no `.scopeproof` review storage, coverage data,
virtual environments, Git metadata directory, Python cache directory, or
bytecode in either distribution. The source distribution intentionally includes
repository documentation, tests, and GitHub configuration; those are source
files, not a packaged `.git` directory.

## Isolated wheel installation

The wheel installed into a fresh Python 3.12 virtual environment outside the
checkout with 47 packages. The isolated CLI reported `scopeproof 0.2.3`.

The installed package then:

- completed the 12-case deterministic benchmark without mismatches or
  must-have False Ready outcomes;
- completed the two-case comparison benchmark with exact Added, Modified,
  Relocated, Removed, and Unchanged counts;
- created and persisted a four-criterion review at `head-demo-002`;
- returned the conservative `blocked` verdict with AC-02, AC-03, and AC-04
  blocking;
- reopened the record through the installed storage/schema path for export;
- produced non-empty Markdown, CSV, and HTML output;
- produced JSON that validated as `ReviewState`, preserving four criteria,
  `head-demo-002`, and the `blocked` verdict; and
- launched the installed workbench on loopback, where
  `/_stcore/health` returned `ok`.

The packaged page contained the candidate's explicit `:focus-visible` rule.
This confirms package inclusion, not successful keyboard-only operation.

### 2026-07-28 security-continuation package check

The later package build contained 98 wheel entries and 519 source-distribution
entries. Its hashes were:

- wheel:
  `6fba59dd2a5ccc334a3ecb5fec2d0ca93ee9ca3943368efd1b7f5c5cad9312ca`;
- source distribution:
  `7c68915dfbd67886ffb652df5df20807b607846d6d0fd559124dde1a187c0a6b`.

The inventory remained free of review storage, coverage data, Git metadata,
virtual environments, caches, and bytecode. The clean installation resolved
GitPython 3.1.57, passed dependency compatibility, ran both installed
benchmarks without mismatches, created a conservative blocked review, exported
four non-empty formats, validated the JSON export as `ReviewState`, and
returned `ok` from the installed workbench health endpoint.

## Historical accessibility matrix

| Check | Result |
| --- | --- |
| Semantic labels and reading order | The 2026-07-31 browser run found accessible names for 119/119 interactive controls and zero duplicate IDs; heading order remains partial browser evidence and no screen reader was used |
| Sampled body and link contrast | Pass at 18.11:1 and 6.58:1 |
| Visible focus rule in installed package | Present; complete keyboard flow not executed |
| Narrow viewport at 390 x 844 and 640 x 720 | Pass for the sampled evidence-matrix route; the later installed check also found no page-level horizontal overflow at 640 x 720 |
| Keyboard-only completed review | Attempted, but the controlled browser did not expose reliable focus activation; not verified |
| VoiceOver, NVDA, or JAWS | Not executed in this environment |
| 200% browser zoom | Attempted, but the controlled browser did not expose a reliable zoom change; not verified |
| WCAG conformance | Not claimed |

The detailed evidence and screenshots are in the
[first-use accessibility audit](../audits/v0.2.3-workbench/accessibility-and-first-use-audit.md).

## Historical checkout gates

- Complete offline suite: 1,633 passed and one intentional skip.
- Combined `scopeproof_core` and `apps` coverage: 95.10%;
  `--fail-under=95` passed.
- Ruff: passed.
- Repository and Action contracts passed 78 tests; alpha and CLI checks passed
  147 tests.
- Deterministic benchmark: 12/12 cases, zero mismatches, zero must-have False
  Ready.
- Comparison benchmark: 2/2 cases, zero mismatches.
- R-002: 20/20 cases, zero failures, zero skips, zero normalized rerun
  mismatches, zero unexpected Ready outcomes, zero immutable-reference errors,
  and zero implementation/test separation errors.

Frozen tracked R-002 hashes remained:

- source manifest:
  `b2acd73b4695ff20ae31723199691374621b0258cccc76f4e807aee66044e4bb`;
- criteria:
  `bb62a7f5bd891322cfcf7ef6681d61dec63b8d2c28078e2aef35a756e1485ece`;
- candidate labels:
  `e1ba509a9569195d68e718708f2585941052a7eeb2099d46a3697963f8a2abe8`;
- recorded result:
  `2fc7115a6ffee3143d0f1d93dca1d51359f40857ec89f1c180d04d7f1e2d359f`;
- summary:
  `e64ce423c0ede72bc3e29f0c23ffc2ba40928b8860191b0553e5cc88a7b75a03`.

The 2026-07-31 R-002 run records its ScopeProof commit as provenance. That
expected provenance field means its complete emitted JSON is not claimed to be
byte-identical to the historical recorded result. The tracked frozen inputs and
record remain byte-identical, and the benchmark's normalized rerun mismatch
count is zero.

## Evidence boundary

These results establish current-environment engineering reproducibility only.
They do not establish correctness of public PRs, participant usability,
customer value, market demand, commercial readiness, external platform
support, or Stage 1–4 progress.
