# ScopeProof v0.2.3 internal candidate

Status: Internal only; not published  
Track: Evidence Quality  
Validation credit: zero

ScopeProof v0.2.3 explains why deterministic evidence retrieval did or did not
produce candidates, makes changed-review decisions easier to revisit, and
classifies the completed R-002 misses without changing the core retrieval or
gate rules.

This candidate is engineering evidence only. It does not prove correctness,
runtime behavior in a target repository, customer demand, accessibility
conformance, or product validation, and it does not advance Stage 1.

## Included

- Validated per-criterion retrieval outcomes and diagnostics.
- Backward-compatible review persistence: historical records do not invent
  diagnostics that were never captured.
- Diagnostics in the workbench, CLI-created reviews, constructed demo, and
  JSON, Markdown, CSV, and HTML exports.
- R-002 miss taxonomy with 14 threshold-rejection cases and one unsupported
  evidence-form case. The frozen inputs and result were not rescored.
- Evidence-delta comparison guidance and an explicit list of prior decisions
  requiring re-review.
- Compact candidate-strength and unresolved-criteria summaries.
- A visible keyboard-focus treatment plus a bounded macOS first-use and
  accessibility engineering audit.

## Preserved boundaries

- Search diagnostics explain retrieval; they are not evidence.
- ScopeProof does not carry reviewer acceptance to a changed PR head.
- Static implementation or test candidates do not establish runtime
  verification.
- R-002 research labels are benchmark-owner labels, not source-owner acceptance
  criteria.
- R-003 remains design-only. Cohort generation requires a separate explicit
  owner approval, followed by later criteria and label approval gates.
- No paid API, LLM, billing, private repository, account system, fork test,
  outreach, email, direct message, generic code review, security scan, or
  automatic fix capability was added.

## Verification state

The internal-candidate checkout gate at
`87e95a76ca4100458465da520049992e8b39af5c` passed:

- 1,591 tests with one intentional skip;
- the configured 95% coverage gate, with exact combined coverage of 95.04%;
- Ruff and repository contracts;
- the 12-case deterministic and two-case comparison benchmarks;
- a clean 20-case R-002 run with zero failures, normalized mismatches,
  unexpected Ready outcomes, immutable-reference errors, or evidence-type
  separation errors;
- frozen tracked R-002 input, result, and summary hashes;
- source and wheel inventory inspection;
- isolated Python 3.12 wheel installation, installed CLI benchmarks,
  representative review persistence, and four export formats; and
- packaged workbench loopback health.

Package artifacts were built from the preceding source-identical package base
`482d1a78f5d3345029d35d33a518ca36bc2f8e29`; the only intervening repository
change added taxonomy test coverage so the exact 95% checkout gate passed.

Only Python 3.12 and macOS were available. Python 3.11, Python 3.13, Windows,
Linux, 200% browser zoom, a completed keyboard-only flow, and a real screen
reader were not executed and are not inferred.

Exact commands, artifact hashes, results, and limitations are in the
[platform and package matrix](v0.2.3-platform-package-matrix.md).

## Publication boundary

Local commits do not publish v0.2.3. No push, pull request, merge, tag, GitHub
Release, notification, or other external action is part of this internal
candidate. Publication remains a separate user-controlled decision after
current-head verification.

## Exact external waiting condition

The inbound-only Stage 1 ledger remains at zero until a non-owner independently
provides a real public PR, public acceptance requirements, source-owner
confirmation, and the exact PR head SHA, then completes a review and records an
outcome. Owner rehearsals, public research cases, and constructed benchmarks
remain engineering evidence only.
