# ScopeProof v0.2.3 PR review map

Date: 2026-07-29
Merged review: [PR #174](https://github.com/YuzeJ21/Scope-Proof/pull/174)
Merge commit: `178df8a1c8034edc4d3240d790e3e4c668dfa82a`
Public draft head at audit start: `2d3bd6afba8435b0bdfce2eb8122ed7bee87eb91`
Base `main` head at audit start: `6414bf31e1f52e8fdf5a8eba1b2e19fd1a4af64d`
Classification: internal engineering review; zero Stage 1 credit

This map makes the large v0.2.3 candidate reviewable by product boundary rather
than commit count. It does not replace line review, CI, runtime verification, or
an owner-controlled merge decision.

## Review lanes

| Lane | Primary files | Intended change | Review result and evidence |
| --- | --- | --- | --- |
| Schemas and deterministic gates | `scopeproof_core/schemas/models.py`, `scopeproof_core/gates/` | Add validated retrieval diagnostics while preserving fail-closed criteria, evidence, resolution, and gate relationships | Diagnostics are validated metadata, not verdict evidence. Gate recomputation and lifecycle consistency remain independent of serialized gate claims. |
| Deterministic retrieval | `scopeproof_core/retrieval/engine.py`, `tests/retrieval/` | Return the established candidates plus one diagnostic per criterion | Existing candidate ordering, IDs, references, scores, and thresholds remain regression-covered. Diagnostics report search facts without changing candidate acceptance rules. |
| Re-review integrity | `scopeproof_core/reviews/comparison.py`, comparison tests and exports | Add Unchanged coverage and expose criteria whose prior decisions require review | A changed head or ruleset never silently carries prior acceptance. Previous and current evidence references remain separate. |
| Persistence and reporting | `scopeproof_core/storage/`, `scopeproof_core/reporting/exporters.py`, schema and reporting tests | Persist new diagnostics in new records and render them in Markdown, JSON, CSV, and HTML | Historical records default to no diagnostics rather than inventing search history. Pydantic validation and spreadsheet-formula escaping remain active. |
| Public GitHub boundaries | `scopeproof_core/github/client.py`, `scopeproof_core/github_action_publisher.py`, GitHub tests | Keep public PR ingestion read-only and the optional Action comment plan head-SHA-idempotent | Public ingestion remains bounded and does not execute target code. The local continuation fixes duplicate comments when an existing marker is after the first 100 comments. The Action remains an advanced, non-default preview. |
| R-002 engineering benchmark | `scopeproof_core/evals/`, `evals/r002/`, `tests/evals/` | Add the frozen 20-case SWE-bench Verified static research baseline and post-hoc miss taxonomy | Raw problem statements, patches, test patches, hints, test names, and source excerpts remain outside tracked artifacts. Network preparation is bounded GET-only. Subprocess use probes only the ScopeProof checkout with `git status` and `git rev-parse`; target repositories are never executed. |
| Workbench first use | `apps/web/app.py`, `tests/apps/`, workbench audit | Show candidate-strength summary, unresolved queue, diagnostics, changed-head warning, and visible focus treatment | The UI continues to distinguish candidates, runtime verification, and human decisions. Existing browser evidence is an owner-operated engineering rehearsal, not accessibility or participant validation. |
| Packaging and CI | `pyproject.toml`, `uv.lock`, `.github/workflows/ci.yml`, package matrix | Package the research command, verify the locked environment, and exercise Python 3.11 | Actions are pinned to full commit SHAs. The local continuation updates transitive GitPython to 3.1.57 after five high-severity advisories. Python 3.11 passed protected PR CI at the audit-start draft head but was not exercised as a local desktop environment. |
| Product and market status | `README.md`, `ROADMAP.md`, `CHANGELOG.md`, release and market documents | Align the candidate, validation, publication, and positioning record | v0.2.3 source is merged to `main` but is not tagged or released. LinkedIn preparation is archived and not authorized. Stage 1 remains at zero and Stages 2–4 remain gated. |

## Confirmed findings repaired locally

1. The active lock contained GitPython 3.1.52 while five open high-severity
   advisories required at least 3.1.55. The local lock now resolves GitPython
   3.1.57, with a regression contract rejecting vulnerable versions.
2. Current documents still described the candidate as local-only, treated
   Python 3.11 as entirely unavailable, and left old LinkedIn publication
   instructions looking active. The documents now distinguish public draft,
   local, CI, historical package, passive-intake, and publication evidence.
3. The optional Action fetched only the first 100 issue comments. A same-head
   marker on a later page therefore produced a duplicate comment. The adapter
   now follows GitHub pagination and updates the existing marker comment.
4. The branch-relative Markdown diff contained trailing whitespace. The local
   continuation removes it without changing product semantics.

## Reviewed limits that remain explicit

- The checked-in `.scopeproof/requirements.txt` and confirmation record are
  intentional advanced-preview inputs inherited from public `main`; they are
  not newly captured local reviews, credentials, or participant data.
- Large or truncated public PR ingestion remains partial and therefore cannot
  produce a Ready result.
- Observed CI describes visible check runs; it does not claim knowledge of every
  repository-required check.
- Manually recorded runtime evidence and reviewer identity remain local human
  assertions rather than authenticated external attestations.
- Python 3.13, Windows and Linux desktop use, completed keyboard-only use, a
  real screen reader, and WCAG conformance remain unverified.
- R-003 cohort generation, criteria confirmation, and label confirmation remain
  separate owner gates. R-002 must not be retuned.
- Stage 1 remains at zero. Benchmarks, CI, owner rehearsals, screenshots, and
  this review provide no customer, market, or commercial validation.

## Verification ledger

- Exact public-draft baseline before local continuation: 1,592 passed and one
  intentional skip on local Python 3.12.
- Repository contracts after dependency and status alignment: 66 passed.
- Focused gate, schema, retrieval, comparison, reporting, GitHub, Action, and
  storage review group: 651 passed and one intentional skip.
- Final full tests, coverage, Ruff, lock, constructed benchmarks, R-002,
  package inventory, isolated installation, exports, and loopback health are
  required on the final local head before this map can support a push decision.
- At source and dependency head
  `5ba6cc6968666c6098b930f5cdffc2503d3d2033`, package inventory, clean-wheel
  installation, dependency compatibility, both installed benchmarks, a
  representative blocked review, four exports, JSON schema validation,
  loopback health, and an installed pointer-flow browser rehearsal passed.
  Keyboard-only completion, a real screen reader, and actual 200% browser zoom
  remain unsupported.

## Owner-controlled publication checklist

- [x] Review and push the final PR #174 head.
- [x] Confirm `verify`, Python 3.11, locked-environment, and CodeQL at exact head
  `e96d9929e651ca36bf178a6fedff0e89e8df3675`.
- [x] Merge PR #174 to `main` as
  `178df8a1c8034edc4d3240d790e3e4c668dfa82a`.
- [x] Close superseded Dependabot PR #175; its GitPython 3.1.54 update is older
  than the merged 3.1.57 lock.
- [ ] Separately verify release alignment and authorize a v0.2.3 tag and GitHub
  Release.
